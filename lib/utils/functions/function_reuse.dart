import 'package:flutter/material.dart';

TextStyle styleDayWiseContainer() => new TextStyle(
      color: Colors.white,
    );

Color getIconColorFromApplicationContext(BuildContext context) {
  return Theme.of(context).primaryColor;
}
